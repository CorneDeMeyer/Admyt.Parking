namespace Parking.Domain.Enum
{
    /// <summary>
    /// Defines the type of a gate, either Entry or Exit
    /// </summary>
    public enum GateType
    {
        Entry = 1,
        Exit = 2
    }
}
